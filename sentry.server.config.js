// Server-side Sentry disabled for Cloudflare Workers compatibility
// Cloudflare Workers don't support Node.js built-ins required by @sentry/node
// Client-side error tracking is still active via sentry.client.config.js
